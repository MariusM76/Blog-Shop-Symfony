{% extends 'base.html.twig' %}

{% block title %}Edit Topic{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
    <h1>Edit Topic</h1>

    {{ include('topic/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <a class="btn btn-outline-info text-dark mx-3 my-3 col-2" href="{{ path('app_topic_index') }}">back to list</a>

    {{ include('topic/_delete_form.html.twig') }}
</div>
{% endblock %}
